#include <libedataserver/libedataserver.h>
#include <stdio.h>
int main() {
    printf("EExtensionClass size: %zu\n", sizeof(EExtensionClass));
    printf("EExtension size: %zu\n", sizeof(EExtension));
    return 0;
}
